{
  "name": "ytheatre",
  "productName": "YTheatre",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . --overwrite --platform=win32 --icon=images/icon.ico --ignore=.git --ignore=.gitignore --prune=true --out=build"
  },
  "author": "Ethan Hazel",
  "license": "ISC",
  "description": "Simple youtube client that uses the TV interface with basic extension support",
  "devDependencies": {
    "electron": "^37.2.2",
    "electron-packager": "^17.1.2"
  }
}
